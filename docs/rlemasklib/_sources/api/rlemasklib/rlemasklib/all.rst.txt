all
===

.. py:function:: rlemasklib.rlemasklib.all(mask)

   Check if all pixels in the mask are foreground.

   :param mask: an RLE mask dictionary

   :returns: True if all pixels are foreground, False otherwise.


.. footbibliography::